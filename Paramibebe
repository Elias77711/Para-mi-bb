<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Te quiero, Stephanie</title>
    <style>
        /* Fondo negro con estrellas sutiles */
        body {
            margin: 0;
            overflow: hidden;
            background: black;
            color: white;
            font-family: 'Arial', sans-serif;
            text-align: center;
        }

        /* Fondo con estrellas */
        .fondo-estrellas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://cdn.pixabay.com/photo/2016/11/21/15/31/astronomy-1845681_960_720.jpg');
            background-size: cover;
            background-position: center;
            opacity: 0.2; /* Para que las estrellas sean sutiles */
            z-index: -1;
        }

        /* Mensaje principal */
        .mensaje {
            position: absolute;
            top: 30%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 4em;
            font-weight: bold;
            text-shadow: 0 0 20px white, 0 0 40px pink;
            animation: brillar 2s infinite alternate;
        }

        /* Animación de brillo */
        @keyframes brillar {
            0% { text-shadow: 0 0 20px white, 0 0 40px pink; }
            100% { text-shadow: 0 0 30px white, 0 0 60px red; }
        }

        /* Mensaje secundario */
        .universo {
            position: absolute;
            bottom: 10%;
            left: 50%;
            transform: translateX(-50%);
            font-size: 2.5em;
            font-weight: bold;
            text-shadow: 0 0 10px white, 0 0 20px cyan;
            animation: flotar 3s infinite alternate ease-in-out;
        }

        /* Animación de flotación */
        @keyframes flotar {
            0% { transform: translateX(-50%) translateY(0); }
            100% { transform: translateX(-50%) translateY(-10px); }
        }

        /* Estilo para los corazones */
        .corazon {
            position: absolute;
            font-size: 2em;
            opacity: 0.8;
            animation: caer 6s linear infinite;
        }

        /* Animación de caída de corazones */
        @keyframes caer {
            0% { transform: translateY(-20vh) rotate(0deg); opacity: 1; }
            100% { transform: translateY(100vh) rotate(360deg); opacity: 0.5; }
        }
    </style>
</head>
<body>

    <!-- Fondo estrellado -->
    <div class="fondo-estrellas"></div>

    <!-- Mensajes -->
    <div class="mensaje">Te quiero, Stephanie ❤️</div>
    <div class="universo">Tú eres mi universo ✨</div>

    <script>
        // Crear corazones rojos de emoji que caen
        for (let i = 0; i < 15; i++) {
            let corazon = document.createElement("div");
            corazon.classList.add("corazon");
            corazon.innerHTML = "❤️";  // Corazón rojo emoji
            document.body.appendChild(corazon);

            // Colocar los corazones en posiciones aleatorias
            corazon.style.left = Math.random() * 100 + "vw";
            corazon.style.top = Math.random() * -20 + "vh";
            corazon.style.animationDuration = (Math.random() * 5 + 3) + "s";
        }
    </script>

</body>
</html>
